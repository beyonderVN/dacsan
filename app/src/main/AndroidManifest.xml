<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ngohoanglong.com.dacsan">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DacsanApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.detail.ProductItemDetailActivity"
            android:theme="@style/AppTheme.Translucent"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".view.login.LoginActivity"
            android:theme="@style/AppTheme.Translucent" />
        <activity
            android:name=".view.welcome.WelcomeActivity"
            android:theme="@style/AppTheme.Translucent" />

        <activity-alias
            android:name=".laucher"
            android:targetActivity=".view.welcome.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".view.signup.SignupActivity"
            android:theme="@style/AppTheme.Translucent" />
        <activity
            android:name=".view.resetpassword.ResetPassword"
            android:theme="@style/AppTheme.Translucent" />
    </application>

</manifest>